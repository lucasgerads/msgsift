# Copy this file to ~/.config/msgsift/credentials.toml and fill in your secrets.
# This file holds secrets ONLY and must never be committed to version control.

[imap_passwords]
personal = "your-imap-app-password"
# work = "your-work-imap-password"

[classifier.cloudflare]
api_token = "your-cloudflare-api-token"
